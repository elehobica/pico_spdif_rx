cmake_minimum_required(VERSION 3.12)

set(PICO_SDK_PATH ${CMAKE_CURRENT_LIST_DIR}/../pico-sdk)
include(pico_sdk_import.cmake)
#include(pico_extras_import.cmake)

set(project_name "pico_spdif_rx" C CXX)
project(${project_name})
set(CMAKE_C_STANDARD 11)
set(CMAKE_CXX_STANDARD 17)

pico_sdk_init()

add_subdirectory(lib/my_pico_stdio_usb)
add_subdirectory(lib/spdif_rx)

set(bin_name "pico_spdif_rx")
add_executable(${bin_name}
    src/main.cpp
)

#pico_enable_stdio_usb(${bin_name} 1)
#pico_enable_stdio_uart(${bin_name} 1)

target_link_libraries(${bin_name}
    pico_stdlib
    #pico_stdio_uart
    my_pico_stdio_usb
    #hardware_uart
    spdif_rx
)

target_include_directories(${bin_name} INTERFACE src)

target_compile_definitions(${bin_name} PRIVATE
    #define for our example code
)

pico_add_extra_outputs(${bin_name})
